# 中源生物 AI 项目甘特图 2026

**交互式甘特图管理系统** - 全栈 Web 应用，支持完整的任务 CRUD 操作和实时数据持久化。

## ✨ 核心特性

### 📊 功能亮点

- ✅ **三级层级结构**: 模块 → 任务 → 子任务
- ✅ **完整 CRUD 操作**: 添加、编辑、删除任务
- ✅ **多时间视图**: 日/周/月/季度切换
- ✅ **拖拽交互**: 拖动调整时间、进度
- ✅ **里程碑标记**: 关键节点可视化
- ✅ **数据持久化**: localStorage 自动保存
- ✅ **导入/导出**: JSON 格式数据交换
- ✅ **任务依赖**: 支持任务关联关系

### 🎯 项目数据

- **总任务数**: 48 个（11个模块 + 37个任务）
- **H1 增长飞轮**: 1-6月，19个任务
- **H2 效率利剑**: 7-12月，18个任务
- **里程碑节点**: 12个关键里程碑

---

## 🚀 快速启动

### 前端应用（推荐）

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装依赖（首次运行）
npm install

# 3. 启动开发服务器
npm run dev

# 4. 访问应用
# 浏览器访问: http://localhost:3004
```

### 构建生产版本

```bash
cd frontend
npm run build

# 生成的文件在 dist/ 目录
# 可直接部署到静态服务器（Vercel、Netlify等）
```

### 传统方式（仅生成静态HTML）

```bash
# 安装 Python 依赖
pip3 install -r requirements.txt

# 生成静态甘特图 HTML
python3 gantt_chart.py

# 或启动 Python 服务器（端口 3003）
python3 server.py
```

---

## 📁 项目结构

```
ai-gantt-chart/
├── frontend/                        # 前端应用
│   ├── src/
│   │   ├── components/
│   │   │   ├── GanttChart/
│   │   │   │   ├── GanttChart.tsx          # DHTMLX 甘特图组件
│   │   │   │   ├── GanttToolbar.tsx        # 工具栏（视图切换、导入/导出）
│   │   │   │   └── TaskEditor.tsx          # 任务编辑弹窗
│   │   │   └── ui/                         # 通用UI组件
│   │   ├── stores/
│   │   │   └── ganttStore.ts               # Zustand 状态管理
│   │   ├── types/
│   │   │   └── gantt.ts                    # TypeScript 类型定义
│   │   ├── utils/                          # 工具函数
│   │   ├── data/
│   │   │   └── initial-data.json           # 初始任务数据
│   │   └── styles/
│   │       └── gantt-theme.css             # 甘特图自定义样式
│   ├── package.json
│   └── vite.config.ts
├── backend/
│   ├── data_exporter.py            # Python数据导出工具
│   └── gantt_chart.py              # 原甘特图生成脚本（保留）
├── server.py                        # 简易Web服务器
├── requirements.txt                 # Python依赖
└── README.md
```

---

## 🛠️ 技术栈

### 前端

- **框架**: React 18 + TypeScript 5
- **构建工具**: Vite 5
- **甘特图库**: DHTMLX Gantt 9.x（专业级甘特图组件）
- **样式**: Tailwind CSS 4
- **图标**: Lucide React
- **状态管理**: Zustand 5
- **日期处理**: date-fns

### 后端（可选）

- **语言**: Python 3.10+
- **数据处理**: Pandas
- **图表**: Plotly（用于静态HTML生成）

---

## 📖 使用指南

### 1. 添加任务

点击工具栏的 **"添加任务"** 按钮，在弹窗中填写：
- 任务名称
- 任务类型（模块/项目、任务、子任务）
- 父任务（可选）
- 开始日期、结束日期
- 负责人
- 进度（0-100%）
- 里程碑标记
- 任务描述

### 2. 编辑任务

双击甘特图中的任务行，打开编辑弹窗修改属性。

### 3. 删除任务

选中任务后按 `Delete` 键，或在编辑弹窗中删除。

### 4. 拖拽调整

- **拖动任务条**: 调整开始时间
- **拖动任务条边缘**: 调整持续时间
- **拖动进度条**: 调整完成进度

### 5. 切换视图

工具栏选择不同的时间视图：
- **日视图**: 按天显示，适合短期任务
- **周视图**: 按周显示，适合月度规划
- **月视图**: 按月显示（默认），适合季度/年度规划
- **季度视图**: 按季度显示，适合长期规划

### 6. 导出/导入数据

- **导出**: 点击 "导出" 按钮，下载 JSON 文件
- **导入**: 点击 "导入" 按钮，选择 JSON 文件上传
- **重置**: 点击 "重置" 恢复到初始的37个任务

---

## 🔧 开发指南

### 数据更新

1. 修改 Python 源数据（`gantt_chart.py` 中的 `tasks_list`）
2. 运行数据导出脚本：

```bash
python3 backend/data_exporter.py
```

3. 生成的 `initial-data.json` 会自动更新到 `frontend/src/data/`

### 类型定义

所有 TypeScript 类型定义在 `frontend/src/types/gantt.ts`：

```typescript
interface GanttTask {
  id: string;
  text: string;
  type: 'project' | 'task' | 'subtask';
  start_date: string;
  end_date: string;
  duration: number;
  progress: number;  // 0-1
  owner: string;
  phase: 'H1' | 'H2' | 'custom';
  priority: 'low' | 'medium' | 'high';
  status: 'planned' | 'in-progress' | 'completed' | 'blocked';
  is_milestone: boolean;
  description?: string;
}
```

### 状态管理

使用 Zustand 进行全局状态管理（`frontend/src/stores/ganttStore.ts`）：

```typescript
const { tasks, links, config, addTask, updateTask, deleteTask } = useGanttStore();
```

### 样式自定义

修改 `frontend/src/styles/gantt-theme.css` 自定义甘特图样式：
- 模块/项目: 蓝色 `#6395ed`
- 任务: 绿色 `#66bb6a`
- 子任务: 橙色 `#ffa726`
- 里程碑: 黄色菱形 `#ffc107`

---

## 🎯 端口配置

- **前端开发服务器**: 3004（可在 `vite.config.ts` 修改）
- **Python 服务器**: 3003（可在 `server.py` 修改）

---

## 🐛 故障排除

### 前端依赖安装失败

```bash
# 清除缓存重新安装
cd frontend
rm -rf node_modules package-lock.json
npm install
```

### 构建错误

```bash
# 检查 TypeScript 错误
npm run build

# 修复常见问题
npm install --legacy-peer-deps
```

### 端口被占用

```bash
# 查看端口占用
lsof -i:3004

# 杀死占用进程
kill -9 <PID>

# 或修改 vite.config.ts 中的端口
```

### 数据丢失

数据存储在浏览器 localStorage，清除浏览器缓存会丢失数据。建议定期导出备份。

---

## 📦 部署

### Vercel 部署（推荐）

```bash
# 1. 安装 Vercel CLI
npm install -g vercel

# 2. 部署
cd frontend
vercel
```

### Netlify 部署

```bash
# 1. 构建
cd frontend
npm run build

# 2. 上传 dist/ 目录到 Netlify
```

### 自托管

```bash
# 构建生产版本
npm run build

# 使用任何静态服务器托管 dist/ 目录
# 例如：nginx、Apache、Caddy
```

---

## 🎨 配色方案

- **模块/项目**: 蓝色系 `#6395ed`
- **任务**: 绿色系 `#66bb6a`
- **子任务**: 橙色系 `#ffa726`
- **里程碑**: 黄色菱形 `#ffc107`
- **背景**: 浅灰色 `#f8f9fa`

---

## 📊 功能路线图

### ✅ 已完成

- [x] 三级层级结构
- [x] 完整 CRUD 操作
- [x] 多时间视图切换
- [x] 拖拽交互
- [x] 里程碑标记
- [x] localStorage 持久化
- [x] 导入/导出 JSON

### 🔄 规划中

- [ ] 后端 API 集成（替换 localStorage）
- [ ] 多用户协作
- [ ] 实时同步（WebSocket）
- [ ] 任务依赖自动调度
- [ ] 关键路径显示
- [ ] 资源分配视图
- [ ] 导出 PDF/PNG
- [ ] 日历集成（Google Calendar）

---

## 📄 许可证

内部项目 - 中源生物企业专用

---

**维护者**: AI项目管理团队
**技术栈**: React + TypeScript + DHTMLX Gantt + Tailwind CSS
**最后更新**: 2026-02-10
